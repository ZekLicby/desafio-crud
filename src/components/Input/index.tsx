import React from "react";
import { InputProps } from "../../types";

export const InputComponent: Function = ({
    type,
    value,
    placeholder,
    onChange,
    onClick,
}: InputProps) => {
    return <></>;
};
